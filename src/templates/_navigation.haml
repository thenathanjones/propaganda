#navigation
  .previous{data: {bind: 'click: previousPage, css: {invisible: !canPrevious()}'}}
  .camera{data: {bind: 'click: capture, css: {invisible: !canCapture()}'}}
  .next{data: {bind: 'click: nextPage, css: {invisible: !canNext()}'}}